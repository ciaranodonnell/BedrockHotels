<UserControl x:Class="Legacy.Desktop.Admin.Employees.EmployeeAddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Legacy.Desktop.Admin.Employees"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Grid.Row="1" Grid.Column="1">
            <Label Content="Address 1"  Target="{Binding ElementName=address1TextBox}"/>
            <TextBox x:Name="address1TextBox" Text="{Binding Address1}" TextChanged="address1TextBox_TextChanged" />
        </StackPanel>
    </Grid>
</UserControl>
